// Type definitions for authentication

/**
 * Basic user interface for authentication purposes
 */
export interface User {
  id: string;
  email: string;
  role: string;
}

/**
 * Auth related types can be added here as needed
 */ 